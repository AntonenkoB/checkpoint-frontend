import { NoRipplePasswordToggle } from './no-ripple-password-toggle';

describe('NoRipplePasswordToggle', () => {
  it('should create an instance', () => {
    const directive = new NoRipplePasswordToggle();
    expect(directive).toBeTruthy();
  });
});
