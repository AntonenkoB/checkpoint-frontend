import { SelectPlaceholder } from "./select-placeholder";

describe("SelectPlaceholder", () => {
  it("should create an instance", () => {
    const directive = new SelectPlaceholder();
    expect(directive).toBeTruthy();
  });
});
