import {TouchFeedbackDirective} from "@shared/directives/touch-feedback";

describe("TouchFeedback", () => {
  it("should create an instance", () => {
    const directive = new TouchFeedbackDirective();
    expect(directive).toBeTruthy();
  });
});
