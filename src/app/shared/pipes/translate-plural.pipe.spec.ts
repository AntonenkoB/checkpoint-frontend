import { TranslatePluralPipe } from "./translate-plural.pipe";

describe("TranslatePluralPipe", () => {
  it("create an instance", () => {
    const pipe = new TranslatePluralPipe();
    expect(pipe).toBeTruthy();
  });
});
